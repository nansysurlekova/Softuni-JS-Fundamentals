function concatenateNames(first, second, delimiter) {
  let firstName = first;
  let secondName = second;
  let currentDelimiter = delimiter;
  console.log(firstName+currentDelimiter+secondName);
}
concatenateNames("Linda", "Terry", "=>");
